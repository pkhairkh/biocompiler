name: biocompiler-ci
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.10
  - pip
  # Bioinformatics tools
  - blast>=2.14
  # Development tools
  - pytest>=7.0
  - pytest-cov>=4.0
  - hypothesis>=6.0
  # pip dependencies
  - pip:
    - -e ".[dev]"
