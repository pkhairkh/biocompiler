biocompiler.optimizer.multigene
================================

.. note::

   The ``biocompiler.optimizer.multigene`` module was **removed**. Multi-gene
   optimization is now performed by calling :func:`biocompiler.optimize_sequence`
   (or :func:`biocompiler.batch_optimize`) once per gene. See
   ``docs/optimizer.rst`` for current usage.
