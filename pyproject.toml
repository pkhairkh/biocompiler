[project]
name = "biocompiler"
version = "1.0.0"
description = "A compiler framework for human protein synthesis using intermediate representations"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.optional-dependencies]
mvp = ["z3-solver>=4.12.0"]
dev = ["pytest>=7.0"]
