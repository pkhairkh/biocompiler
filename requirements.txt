# BioCompiler Dependencies
# See pyproject.toml for the complete, authoritative dependency specification.
# Install options:
#   pip install -e .                   # Core dependencies only
#   pip install -e ".[solver]"         # CSP solver backends (OR-Tools + Z3)
#   pip install -e ".[all]"            # All optional dependencies
#   pip install -e ".[dev]"            # Development dependencies (pytest, ruff, mypy, etc.)
